version: 2

models:
  - name: dim_product
    description: Dimensão de produtos
    columns:
      - name: product_sk
        description: primary key of the product
        tests:
          - unique
          - not_null